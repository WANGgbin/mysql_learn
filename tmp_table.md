# 临时表解决什么问题

针对一些复杂查询，往往需要对结果进行去重、排序、分组等操作。
比如：
- 分库分表场景

    如果需要跨库查询，而且要对查询的结果进行排序或者去重。怎么实现呢？

- mysql 内部优化

    在执行 group by(假设没有合适索引)、union 等操作时，会创建合理的临时表来存储中间数据，然后利用表的特性完成分组、去重等操作。

# 怎么解决的

通过创建临时表，利用 mysql 表的特性完成去重(唯一主键)、分组(索引天然分组)、排序(索引天然有序)等特性。

临时表按创建者分为两种；

- 用户临时表

    用户可以手动通过 `create temporary table ...` 语句来创建临时表。用户临时表特性如下：
  - 用户临时表是 session 纬度的，不同 session 可以创建不同的临时表。
  - 在 session 退出时(正常/异常)，session 关联的临时表会被删除。

- 内部临时表
    
    由 mysql 自己创建。

按照临时表在内存中还是磁盘中，又可以分为：

- 内存临时表

    如果数据量比较小(由变量 tmp_table_size 控制)，则直接创建 memory 类型的表。

- 磁盘临时表

    如果数据量比较大，则创建 innodb 类型的磁盘临时表。

# memory 引擎

memory 引擎特性如下：
- 完全基于内存，实例重启，数据丢失，没有持久化机制。
- 存储上，数据和索引是分开的，索引默认是哈希索引。
- 不支持行级锁，仅支持表级别锁。
- 支持数据量有限。

那什么场景适合使用 memory 引擎呢？

内存临时表。

因为内存临时表特性如下：
- 大小有限
- 无需持久化
- 只有一个线程访问，没有并发问题

所以使用 memory 很适合。

一个典型的例子就是在执行 join 的时候，如果被驱动表无法利用索引，我们可以将被驱动表的数据查出来，插入到一个临时 memory 内存表中，该临时表
的主键为 on 条件中的字段。

然后对于驱动表中的每一条记录，直接通过 memory 表的哈希索引来查找匹配的记录，大大增加了 join 的效率。

这个例子即是 memory 引擎的典型使用场景，也是临时表这种思想的应用。