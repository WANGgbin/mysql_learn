对于 mysql 表中的某个列，我们可以指定为 auto_increment，这样后续在插入数据的时候，mysql 内部会自动给这个字段赋值。

这里有以下几个变量决定了，auto_increment 如何赋值：

- auto_increment_offset

    自增的起始值。默认值为 1.

- auto_increment_increment

    自增的步长。默认值为 1.

# 如何分配

由 innodb_autoinc_lock_mode 决定了自增值的分配策略，该变量取值如下：

- 0

    语句级别。语句执行的时候，加 auto_inc 锁(表级别锁)，只有语句执行完毕才释放。这种方式并发度比较低。

- 1

    对于能精确知道插入行数的 insert 语句，申请完序号后，立即释放。
    对于无法确定行数的 insert 语句，还是在语句执行完，才释放锁。

- 2
    
    无论什么类型的 insert 语句，都是在申请完毕后释放。

我们重点来看看 innodb_autoinc_lock_mode = 2 的这种配置。

有两个问题：
- 主从一致性问题

    如果 binlog 格式为 statement，同时执行两个插入语句，比如一个批量插入，一个插入一行记录。主从上，无法保证，这两个事务并行执行的
时候对 autoinc_lock 加锁的顺序是一样的。就会导致主从不一致。

    因此，常常将 innodb_autoinc_lock_mode = 2 与 binlog_format = row 结合使用。

- 插入大批量语句时，如何优化性能

    既然是插入一条语句，就申请一次锁。如果 insert ... select ... 方式插入大批量语句，每插入一行语句，就需要加一次锁。性能较差，如何优化？

    mysql 思路是，同一个语句中，每一次申请的序号数量都是上次申请的两倍，比如：1个值、2个值、4个值 ...。这样大大降低了申请锁的次数。

# 自增值为啥不连续

两类原因。

- 插入事务回滚

    如果一个插入事务，执行到一半，回滚了。已经申请的自增值就会导致不连续。

- innodb_autoinc_lock_mode = 2

    因为同一个语句中每次申请都是上一次的两倍，因此可能导致多申请了一部分自增值，这也会导致不连续。

# 如何存储

- mysql 5.7 及以前

    维护在内存中。系统重启时，通过访问表最近一条记录，获取自增主键值，然后以此为基础，继续分配。

- mysql 8.0

    存储在数据字典中，也就是在系统表空间的表中。